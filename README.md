Java User Registration & Login System
A Java console app that handles secure user registration and login validation.
how it works 
**Username rules:** Must be 5 characters or less an include an underscore (`_).
**Password criteria:** Requires at least 8 characters, one uppercase letter, one digit, and one special character.
**Input loops:** keeps prompting the user until all security formatting requirements are met.
